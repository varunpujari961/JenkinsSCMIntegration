/*package Validations;

import org.openqa.selenium.WebDriver;

import ObjectsRepository.*;

public class NielsenValidations {
	
	public String validateSETParameter(WebDriver driver) {
		CreateSegmentPage createSegment = new CreateSegmentPage(driver);
		return createSegment.ddl1_QueryParameter.getText();
	}
	
	public String validateSETParameter_AMRLD(WebDriver driver)
	{
		CreateSegmentPage createSegment = new CreateSegmentPage(driver);
		return createSegment.ddl1_QueryParameter_Set.getText();
	}
	
	public String validateIntegerParameter(WebDriver driver) {
		CreateSegmentPage createSegment = new CreateSegmentPage(driver);
		return createSegment.ddl1_QueryParameter.getText();
	}
	
	public String validateIntegerValueParameter(WebDriver driver) {
		CreateSegmentPage createSegment = new CreateSegmentPage(driver);
		return createSegment.txt_QueryParameter_Integer.getText();
	}
	
	public String getSegmentErrorMessage(WebDriver driver) {
		CreateSegmentPage createSegment = new CreateSegmentPage(driver);
		return createSegment.SegmentErrorMessge.getText();
	}

}
*/